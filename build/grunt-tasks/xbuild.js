var task = {
    build: {
        options: {
            sln_file: "TaskScheduler.sln",
            configuration: "Release",
            target_framework: "v4.0"
        }
    }
};

module.exports = task;
